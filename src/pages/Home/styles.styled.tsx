import styled from "styled-components";

export const Main = styled.main`
  overflow: hidden;
  padding: 4rem 1rem;
  min-height: 100vh;
`;

export const Container = styled.div`
  max-width: 1920px;
  margin: 0 auto;

  header {
    max-width: 1000px;
    margin: auto;
  }
`;
